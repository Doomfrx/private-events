<div class="container">
  <h2>Attendees for <%= link_to @event.name, @event %></h2>
  <ul class="registrations">
    <% @registrations.each do |r| %>
      <li>
        <%= r.user.name%>
      </li>
    <% end %>
  </ul>
<div>